name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-native:
    uses: ./.github/workflows/reusable-build-native.yml

  test:
    needs: build-native
    uses: ./.github/workflows/reusable-test.yml
